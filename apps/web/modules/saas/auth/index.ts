// Server-safe exports only
// For client components/hooks, import from "@saas/auth/client"

// Constants and types (server-safe)
export {
	type OAuthProvider,
	oAuthProviders,
} from "./constants/oauth-providers";
