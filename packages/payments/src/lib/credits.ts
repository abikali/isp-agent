/**
 * AI credits initialization - removed.
 * Kept as no-op for any remaining callers.
 */
export async function initializeCreditsForPurchase(
	_organizationId: string | null,
	_productId: string,
): Promise<void> {
	// No-op: AI credit system removed
}
